# Optional nginx example: proxy from Tailscale to the CF app (internal or public URL).
# Use when you want a single hostname (e.g. openclaw.your-tailnet.ts.net) instead of
# the platform app URL. Replace OPENCLAW_APP_URL with your app URL (e.g. http://openclaw.apps.internal:8080).

# server {
#     listen 443 ssl;
#     server_name openclaw.your-tailnet.ts.net;
#     ssl_certificate /path/to/cert;
#     ssl_certificate_key /path/to/key;
#
#     location / {
#         proxy_pass OPENCLAW_APP_URL;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
